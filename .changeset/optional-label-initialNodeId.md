---
"@statelyai/graph": minor
---

- Make `Graph.initialNodeId`, `GraphNode.label`, and `GraphEdge.label` optional on resolved types for easier consumer usage
- Add `createGraphNode()` and `createGraphEdge()` helpers that resolve defaults from config
